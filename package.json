{
  "name": "sync-ld-flags",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint '**/*.js' && npx prettier --check '**/*.js'",
    "lint:fix": "eslint '**/*.js' --fix && npx prettier --write '**/*.js'",
    "sync": "./sync-ld-flags",
    "test": "mocha"
  },
  "fig": {
    "sync": {
      "description": "Sync flags",
      "icon": "🔄",
      "priority": 100
    }
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^8.3.0",
    "fast-json-patch": "^3.1.0",
    "request": "^2.72.0"
  },
  "devDependencies": {
    "eslint": "^8.8.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-prettier": "^4.0.0",
    "mocha": "^9.1.3",
    "prettier": "^2.5.1",
    "rewire": "^5.0.0"
  }
}
